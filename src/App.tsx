import { MantineProvider } from "@mantine/core";

import "@mantine/core/styles.css";

const App = () => {
  return (
    <MantineProvider>
      <div>App</div>
    </MantineProvider>
  );
};

export default App;
